#!/usr/bin/make -f
# -*- makefile -*-

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1
export DH_GOPKG := github.com/jsouthworth/seriatim
export DH_GOLANG_EXCLUDES := cmd/example

%:
	dh $@ --buildsystem=golang --with=golang
